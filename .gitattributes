# 统一行尾：仓库内存 LF，避免 Windows 下检出的 CRLF 破坏 Linux 上的可执行脚本
* text=auto eol=lf

# shell 脚本必须 LF（CRLF 会导致 "bad interpreter: /usr/bin/env bash^M"）
*.sh text eol=lf
Dockerfile text eol=lf
Caddyfile.example text eol=lf
docker-compose*.yml text eol=lf

# 二进制文件不做转换
*.png binary
*.jpg binary
*.woff2 binary

# 让 GitHub 语言统计更准确
*.js text
*.mjs text
*.md text
*.sql text
